description: "Generalization arises with a balanced representation space."
url: "https://la0ka1.github.io"
baseurl: "/diffusion-gen-from-rep"
repository: "la0ka1/diffusion-gen-from-rep"

remote_theme: "mmistakes/minimal-mistakes"
minimal_mistakes_skin: "air"   # try: "air", "dark", "mint", "sunrise"

plugins:
  - jekyll-include-cache

markdown: kramdown
kramdown:
  input: GFM

author:
  name: "Zekai Zhang"

defaults:
  - scope:
      path: ""
    values:
      layout: single
      classes: wide
      toc: false
      toc_sticky: false
